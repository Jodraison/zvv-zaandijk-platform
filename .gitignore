# Dependencies & build
node_modules
.next
.next-*

# Secrets — never commit real values (use .env.example as template)
.env
.env.local
.env.*
!.env.example

# Local QA auth / dumps (may contain session cookies)
.review-auth/

.data
*.log

.vercel
.review-screenshots/

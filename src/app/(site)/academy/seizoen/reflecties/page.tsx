import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-02 — `/seizoen/reflecties` (S-61). Engine = WP09. */
export default function AcademySeizoenReflectiesPage() {
  return (
    <AcademyRouteStub
      title="Seizoen · Reflecties"
      description="Reflectie-overzicht volgt later (WP09). Deep-link entry is actief."
    />
  );
}

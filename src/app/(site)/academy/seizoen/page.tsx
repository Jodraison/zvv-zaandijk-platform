import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Seizoen tab entry (engine = WP09). */
export default function AcademySeizoenPage() {
  return (
    <AcademyRouteStub
      title="Seizoen"
      description="Mijn Seizoen volgt later. Route-entry is actief."
    />
  );
}

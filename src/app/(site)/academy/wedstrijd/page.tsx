import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Wedstrijd tab entry (engine = WP08). */
export default function AcademyWedstrijdPage() {
  return (
    <AcademyRouteStub
      title="Wedstrijd"
      description="Wedstrijddag-hub volgt later. Route-entry is actief."
    />
  );
}

import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — S-20 Positie entry (dashboard widgets = WP04). */
export default function AcademyPositiePage() {
  return (
    <AcademyRouteStub
      title="Positie"
      description="Positie-dashboard volgt in een latere Sprint-taak. Route-entry is actief."
    />
  );
}

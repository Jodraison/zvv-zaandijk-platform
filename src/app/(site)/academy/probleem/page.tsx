import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Probleem tab entry (engine = WP07). */
export default function AcademyProbleemPage() {
  return (
    <AcademyRouteStub
      title="Probleem"
      description="Probleem-navigator volgt later. Route-entry is actief."
    />
  );
}

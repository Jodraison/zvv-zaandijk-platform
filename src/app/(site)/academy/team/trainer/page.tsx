import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Trainer role entry (module = WP11). */
export default function AcademyTrainerPage() {
  return (
    <AcademyRouteStub
      title="Trainer"
      description="Trainer-dashboard volgt later. Route-entry is actief."
    />
  );
}

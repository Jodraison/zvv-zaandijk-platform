import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Captain role entry (module = WP10). */
export default function AcademyCaptainPage() {
  return (
    <AcademyRouteStub
      title="Captain"
      description="Captain-dashboard volgt later. Route-entry is actief."
    />
  );
}

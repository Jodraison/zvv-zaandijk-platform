import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Zoek entry (overlay/engine = WP12 / T-12). Closes header 404. */
export default function AcademyZoekPage() {
  return (
    <AcademyRouteStub
      title="Zoeken"
      description="Zoekfunctie volgt later. Route-entry is actief zodat de headerlink geen 404 geeft."
    />
  );
}

import { AcademyRouteStub } from "@/components/academy/academy-route-stub";

/** T-02-01 — Situatie tab entry (engine = WP06). */
export default function AcademySituatiePage() {
  return (
    <AcademyRouteStub
      title="Situatie"
      description="Situatie-navigator volgt later. Route-entry is actief."
    />
  );
}
